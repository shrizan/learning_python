first_name = "First"
last_name = "last"
full_name = f"{first_name} {last_name}"
print(full_name)

# other function can be called from the curly brackets inside of the string
full_name_length_with_white_space = f"Length: {len(full_name)}"
print(full_name_length_with_white_space)
